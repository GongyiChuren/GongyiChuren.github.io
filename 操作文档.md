# 操作文档（本地编辑与发布）

本文档面向仓库根目录（`D:\GitHub-working\GongyiChuren.github.io`），以下路径均为**仓库根目录下的相对路径**（以 `/` 开头）。

---

## 1. 本地预览

首次或依赖有变化时：
```bash
pnpm install
```

开发预览：
```bash
pnpm dev
```
浏览器打开：
```
http://localhost:4321
```

---

## 2. 文章管理（新增/修改）

文章目录：
```
/src/content/posts/
```

### 2.1 新增文章（推荐流程）
1. 复制一篇现有文章（`.md`）作为模板
2. 修改文件名，建议格式：`YYYY-MM-DD-标题.md`
3. 编辑文章头部 `frontmatter` 与正文

示例模板：
```md
---
title: "文章标题"
published: 2026-01-26
description: "一句话摘要"
tags:
  - "标签1"
  - "标签2"
category: "分类名"
draft: false
---

正文从这里开始。
```

### 2.2 修改文章
直接修改对应 `.md` 文件即可。

### 2.3 草稿
将 `draft: true` 可隐藏文章（构建时不发布）。

---

## 3. 常用页面修改

### 3.1 关于我
文件：
```
/src/content/spec/about.md
```

### 3.2 友链
文件：
```
/src/content/spec/friends.md
```

### 3.3 留言板
文件：
```
/src/content/spec/guestbook.md
```

---

## 4. 导航栏/页脚/基础配置

### 4.1 站点信息与 Logo
文件：
```
/src/config/siteConfig.ts
```
常用字段：
- `title` / `subtitle` / `description`
- `site_url`
- `navbar.logo`（图标或图片）

### 4.2 导航栏菜单
文件：
```
/src/config/navBarConfig.ts
```

### 4.3 页脚
文件：
```
/src/config/footerConfig.ts
/src/config/FooterConfig.html
```

---

## 5. 图片与静态资源

推荐两种方式：

1) 文章图片（推荐）
```
/src/content/posts/images/
```
引用方式（示例）：
```
![示例](/posts/images/xxx.webp)
```

2) 全站公共资源
```
/public/assets/images/
```
引用方式：
```
/assets/images/xxx.webp
```

---

## 6. 本地构建

```bash
pnpm build
```
构建产物在 `dist/`（一般不需要手动发布，Actions 会自动完成）。

---

## 7. 发布到线上

只需要提交源码并推送：
```bash
git add -A
git commit -m "update"
git push
```

GitHub Actions 会自动构建并发布到 Pages。

---

## 8. 常见问题

- **页面样式错乱**：确认 Actions 成功；检查 `/_astro/` 能否访问
- **图片不显示**：确认路径以 `/assets/` 或 `/posts/images/` 开头
- **文章没出现**：检查 `draft: false`，以及 frontmatter 格式是否正确
